# Milestone 1 requires no environment variables.
#
# A later milestone may add an experimental GPT-5.6 paper-analysis service.
# Document its server-only variables here when that service is implemented.
# Never expose a provider key through a NEXT_PUBLIC_ variable.
# OPENAI_API_KEY=

# Optional: canonical production origin for absolute social-preview metadata.
# SITE_URL=https://paper-to-prototype.example
